{
    "version": 2,
    "buildCommand": "npm install --prefix webapp && npm run build --prefix webapp",
    "outputDirectory": "webapp/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}